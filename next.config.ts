import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['tm.ibxk.com.br', 'NEXT_PUBLIC_BASE_URL=https://chocolatesensual.vercel.app'],
  },
};

export default nextConfig;
